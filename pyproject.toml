[project]
name = "pydantic_for_validation"
version = "0.1.0"
description = "A python library for data validation & serialization"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "pydantic[email]>=2.12.5",
    "requests>=2.32.5",
]
